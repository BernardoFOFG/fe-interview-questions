import SectionQuestions from '../components/section_questions';
import Layout from '../layout';

<Layout>
	<SectionQuestions title="Perguntas sobre Códigos" />

<div className="space-y-4">
	<h3 className='text-xl'>Pergunta: Qual é a saida desse trecho?</h3>
```javascript
  let a = 5;
  let b = 10;
  console.log(a + b);
```

<h3 className='text-xl'>Pergunta: Qual é a saída desse trecho?</h3> 
```javascript
	let x = 3;
	let y = '7';
	console.log(x + y);
```

<h3 className='text-xl'>Pergunta: Qual é a saída desse trecho?</h3>
```javascript
const isEven = num => num % 2 === 0;
console.log(isEven(4));
``` 

<h3 className='text-xl'>Pergunta: Qual é a saída desse trecho?</h3> 
```javascript
let fruits = ['apple', 'banana', 'cherry'];
console.log(fruits.length);
```

<h3 className='text-xl'>Pergunta: Qual é a saída desse trecho?</h3> 
```javascript 
let num = 8; console.log(num > 10 ? 'Greater' : 'Smaller'); 
``` 

<h3 className='text-xl'>Pergunta: Qual é a saída desse trecho?</h3> 
```javascript 
const greet = name => `Hello, ${name}!`;
console.log(greet('John'));
``` 

<h3 className='text-xl'>Pergunta: Qual é a saída desse trecho?</h3>
```javascript 
let counter = 0; 
for (let i = 0; i < 5; i++) {
counter += i; 
} 
console.log(counter); 
 ```
 
 <h3 className='text-xl'>Pergunta: Qual é a saída desse trecho?</h3> 
 ```javascript 
let arr = [1, 2, 3];
arr.push(4); 
console.log(arr); 
``` 

<h3 className='text-xl'>Pergunta: Qual é a saída desse trecho?</h3> 
```javascript let isValid = false;
console.log(!isValid);
``` 

<h3 className='text-xl'>Pergunta: Qual é a saída desse trecho?</h3> 
```javascript const add = (a, b) => a + b; 
console.log(add(5, 7)); 
``` 

<h3 className='text-xl'>Pergunta: Qual é a saída desse trecho?</h3> 
```javascript let str = 'Hello World'; 
console.log(str.toUpperCase()); 
``` 

<h3 className='text-xl'>Pergunta: Qual é a saída desse trecho?</h3> 
```javascript 
let num = 6; num *= 2; 
console.log(num); 
``` 

<h3 className='text-xl'>Pergunta: Qual é a saída desse trecho?</h3> 
```javascript let animals = ['dog', 'cat', 'bird']; 
animals[1] = 'fish'; 
console.log(animals); 
``` 

<h3 className='text-xl'>Pergunta: Qual é a saída desse trecho?</h3> 
```javascript const person = { name: 'Alice', age: 25 }; 
console.log(person.name); 
``` 

<h3 className='text-xl'>Pergunta: Qual é a saída desse trecho?</h3>
 ```javascript const square = num => num ** 2;
  console.log(square(3));
 ```
</div>
</Layout>
