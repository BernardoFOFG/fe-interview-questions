import SectionQuestions from '../components/section_questions'
import Layout from '../layout'

<Layout>
  <SectionQuestions title="Perguntas sobre JavaScript" />
  - Explique delegação de eventos.
  - Explique como o `this` funciona no JavaScript.
  - Você pode dar um exemplo de uma das maneiras que o uso de `this` mudou no ES6?
  - Explique como funciona a herança prototipal.
  - Qual é a diferença entre uma variável que é: null, undefined ou undeclared?
  - Como você verificaria qualquer um desses estados?
  - O que é um closure, e como/por que você usaria um?
  - Quais construções de linguagem você usa para iterar sobre propriedades de objetos e itens de arrays?
  - Você pode descrever a principal diferença entre os métodos `Array.forEach()` e `Array.map()` e por que escolheria um em vez do outro?
  - Você pode descrever outros métodos populares para iterar sobre arrays?
  - Qual é um caso de uso típico para funções anônimas?
  - Qual é a diferença entre objetos host e objetos nativos?
  - Explique a diferença entre: `function Person(){}`, `var person = Person()`, e `var person = new Person()`?
  - Explique as diferenças no uso de `foo` entre `function foo() {}` e `var foo = function() {}`.
  - Você pode explicar o que `Function.call` e `Function.apply` fazem? Qual é a diferença notável entre os dois?
  - Explique `Function.prototype.bind`.
  - Qual é a diferença entre detecção de recursos, inferência de recursos, e uso da string UA?
  - Explique o 'hoisting'.
  - O que é coerção de tipos? Quais são as armadilhas comuns ao confiar na coerção de tipos no código JavaScript?
  - Descreva bubbling de eventos.
  - Descreva captura de eventos.
  - Qual é a diferença entre um 'atributo' e uma 'propriedade'?
  - Quais são os prós e contras de estender objetos nativos do JavaScript?
  - Qual é a diferença entre `==` e `===`?
  - Explique a política de mesma origem em relação ao JavaScript.
  - Por que é chamado de operador ternário? O que a palavra 'Ternário' indica?
  - O que é o 'strict mode'? Quais são as vantagens/desvantagens de usá-lo?
  - Quais são as vantagens/desvantagens de escrever código JavaScript em uma linguagem que compila para JavaScript?
  - Quais ferramentas e técnicas você usa para depurar código JavaScript?
  - Explique a diferença entre objetos mutáveis e imutáveis.
  - Qual é um exemplo de um objeto imutável no JavaScript?
  - Quais são os prós e contras da imutabilidade?
  - Como você pode alcançar imutabilidade no seu próprio código?
  - Explique a diferença entre funções síncronas e assíncronas.
  - O que é o event loop?
  - Qual é a diferença entre call stack e task queue?
  - Quais são as diferenças entre variáveis criadas usando `let`, `var` ou `const`?
  - Você pode mudar uma propriedade de um objeto definido via `const`? Como você pode mudar esse comportamento?
  - Quais são as diferenças entre classes no ES6 e construtores de função no ES5?
  - Você pode fornecer um caso de uso para a nova sintaxe de função arrow `=>`? Como essa nova sintaxe difere de outras funções?
  - Qual é a vantagem de usar a sintaxe arrow para um método em um construtor?
  - Qual é a definição de uma função de ordem superior?
  - Você pode dar um exemplo de destruturação de um objeto ou array?
  - Você pode dar um exemplo de geração de uma string com Template Literals do ES6?
  - Você pode dar um exemplo de uma função curry e por que essa sintaxe oferece uma vantagem?
  - Quais são os benefícios de usar a sintaxe spread e como ela difere da sintaxe rest?
  - Como você pode compartilhar código entre arquivos?
  - Por que você pode querer criar membros estáticos de uma classe?
  - Qual é a diferença entre os loops `while` e `do-while` no JavaScript?
  - O que é uma promise? Onde e como você usaria uma promise?
  - Discuta como você pode usar os princípios de Programação Orientada a Objetos ao codificar com JavaScript.
  - Qual é a diferença entre `event.target` e `event.currentTarget`?
  - Qual é a diferença entre `event.preventDefault()` e `event.stopPropagation()`?
</Layout>
